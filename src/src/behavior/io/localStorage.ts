const PREFIX = "tactical-fulcrum-editor-"
export const LOCAL_STORAGE_KEY_ENEMIES = `${PREFIX}enemies`
export const LOCAL_STORAGE_KEY_LEVELS = `${PREFIX}rooms`
export const LOCAL_STORAGE_CURRENT_MAP = `${PREFIX}current-map`
